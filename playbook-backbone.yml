---
- hosts: internal

  roles:
  - dnsmasq

  post_tasks:
  - name: Check
    ansible.builtin.command: echo check
    notify: Check

- hosts: all

  roles:
  - systemd_resolved

  - nginx
  - nginx_site_http_redirector

  post_tasks:
  - name: Check
    ansible.builtin.command: echo check
    notify: Check

- hosts: internal

  roles:
  - caddy
  - caddy_site_acme_server

  post_tasks:
  - name: Check
    ansible.builtin.command: echo check
    notify: Check
