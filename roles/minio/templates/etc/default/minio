SERVER_LISTEN_ADDRESS=127.0.0.1:{{ minio_server_listen_port }}
CONSOLE_LISTEN_ADDRESS=127.0.0.1:{{ minio_console_listen_port }}

MINIO_VOLUMES=/var/lib/minio/volumes

MINIO_ROOT_USER={{ minio_root_user }}
MINIO_ROOT_PASSWORD={{ minio_root_password }}

MINIO_PROMETHEUS_AUTH_TYPE=public

MINIO_SERVER_URL={{ minio_server_url }}
MINIO_BROWSER=off
