{% for chain_name, entry in nftables_accept_ports_with_addrs.items() %}
chain accept_ports_with_addrs_{{ chain_name }} {
{% for port in entry.tcp_ports %}
  tcp dport { {{ port }} } ct state new accept;
{% endfor %}
}
{% endfor %}
