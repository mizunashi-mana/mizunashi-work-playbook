{% for chain_name, entry in nftables_accept_ports_with_addrs.items() %}
{% for addr in entry.source_addrs %}
ip saddr {{ addr }} jump accept_ports_with_addrs_{{ chain_name }};
{% endfor %}
{% endfor %}
