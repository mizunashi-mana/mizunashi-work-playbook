---
- hosts: all

  roles:
  - base
  - systemd
  - network
  - sudo
  - ca_certs
  - workuser_setup
  - openssh_server

  post_tasks:
  - name: Check
    ansible.builtin.command: echo check
    notify: Check
